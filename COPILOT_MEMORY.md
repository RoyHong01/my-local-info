# COPILOT_MEMORY.md — 픽앤조이 작업 메모

이 파일은 GitHub Copilot 작업 기준의 운영 메모입니다.
Copilot 기준 문서는 `.github/copilot-instructions.md`와 `WORK_LOG.md`를 함께 관리합니다.

## 시작 전 체크리스트

1. `.github/copilot-instructions.md` 확인
2. 이 파일(`COPILOT_MEMORY.md`) 확인
3. 최근 작업 이력(`WORK_LOG.md`) 확인

## 운영 규칙

- 세션 시작 시 위 3개 파일의 최신 상태를 확인한다.
- 세션 종료 전 사용자 확인 여부와 관계없이, 작업 이력이 있으면 문서/메모리 동기화를 수행한다.
- 업데이트 시 최소 `WORK_LOG.md`, `.github/copilot-instructions.md`, `COPILOT_MEMORY.md`를 함께 반영한다.
- 작업 완료 후 `WORK_LOG.md`와 메모리 파일(`COPILOT_MEMORY.md`, `PROJECT_MEMORY.md`)을 자동으로 업데이트한다. 별도 사용자 확인 질문은 하지 않는다.
- 코드 변경 후에는 반드시 `npm run build`로 검증한다.
- 코드 작업 완료 시 반드시 `git add → git commit → git push`를 수행한다.
- **작업 종료 체크리스트(필수)**: build 성공 + commit/push 완료 + 3개 문서 동기화

## 핵심 프로젝트 요약

- 사이트: 픽앤조이 (`https://pick-n-joy.com`)
- 스택: Next.js 16 App Router + TypeScript + Tailwind CSS v4
- 배포: Cloudflare Pages + GitHub Actions(매일 04:00 KST)
- 데이터 소스: 공공데이터포털 + 한국관광공사 TourAPI + Claude API(claude-haiku-4-5)

## 최근 변경 요약 (압축판)

- 상세 이력은 `WORK_LOG.md`를 기준 문서로 사용하고, 이 파일은 작업 규칙과 최신 상태만 유지한다.
- 2026-04-14 핵심 반영(추가):
  - **Safari 사이드바 겹침 대응**: 우측 사이드바는 공용 `StickySidebar` 컴포넌트로 렌더링되고, footer 직전 `sticky -> absolute` 전환으로 콘텐츠/푸터 겹침을 방지.
  - **향후 수정 주의점**: 우측 사이드바 수정 시 `StickySidebar.tsx`, `#site-footer`, 부모 `overflow-visible`, `-webkit-sticky` 스타일을 함께 확인해야 함.
- 2026-04-14 핵심 반영(추가):
  - **맛집 생성 로컬 안정성 보강**: `scripts/generate-life-restaurant-posts.mjs`에서 `.env.local` 자동 로드를 추가해 수동 실행 시 `GEMINI_API_KEY` 누락 실패를 줄임.
  - 이미 주입된 process env는 유지해 Actions 환경과 충돌하지 않도록 구성.
- 2026-04-14 핵심 반영(추가):
  - **초이스 카드 썸네일 복구**: `2026-04-14-choice-living-2026-04-14`의 로컬 이미지 경로가 실제 파일과 불일치해 카드 이미지가 깨지던 문제를 수정.
  - **재발 방지 로직**: `src/lib/life-choice.ts`에서 `/images/...` 경로 존재 여부를 확인하고, 파일이 없으면 `coupangBannerImage`로 자동 fallback.
  - **생성기 보강**: `scripts/generate-choice-post.js`에서 frontmatter `image` 계산 시 기본값을 배너 이미지까지 보강해 누락 재발률을 낮춤.
- 2026-04-14 핵심 반영(추가):
  - **수동 복구 배포 완료**: 당일 스케줄 누락분을 수동으로 복구해 초이스 1건(생활) + 맛집 3건(서울/인천/경기) 생성 후 빌드/푸시까지 완료.
  - **생성 파일**: `2026-04-14-choice-living-2026-04-14.md`, `2026-04-14-seongsu-restaurant-23279805.md`, `2026-04-14-songdo-restaurant-1045132880.md`, `2026-04-14-pangyo-restaurant-1370916394.md`.
  - **연동 이력 반영**: `scripts/data/recommended-products.json`에 초이스 상품 히스토리 갱신.
- 2026-04-14 핵심 반영(추가):
  - **실패 RCA 확정**: 2026-04-14 스케줄 실패는 `generate_choice`에서 후보 0개로 실패한 것이 1차 원인, 이후 3단계 맛집이 `skipped`된 것은 step failure 전파 구조가 원인.
  - **실패 격리 적용**: `.github/workflows/deploy.yml`의 `generate_choice`를 `continue-on-error: true`로 전환해 초이스 실패가 맛집 단계 실행을 막지 않도록 수정.
  - **성공률 보강**: `scripts/generate-choice-posts-auto.js`에 테마별 백업 키워드 병합 재시도(1회) 추가.
- 2026-04-14 핵심 반영(추가):
  - **쿠팡 limit 실원인 확인**: Search API는 `limit > 10`이면 HTTP 200이어도 `limit is out of range`를 반환하므로 Top 50 단일 호출 방식은 실패함.
  - **후보풀 수집 방식 전환**: `scripts/generate-choice-post.js`를 `10개 x 다중 영어 키워드` 기반 후보풀 수집 구조로 조정해 약 50개 규모 탐색을 유지.
  - **선정 fallback 현실화**: 품질 메타데이터가 부족한 경우에도 `rank <= 10` bestseller를 보충 후보로 허용해 생활 테마 로컬 자동 생성 성공을 확인.
- 2026-04-13 핵심 반영(추가):
  - **초이스 전용 커밋 단계 추가**: `.github/workflows/deploy.yml`에서 초이스 생성 직후 `src/content/life`와 `scripts/data/recommended-products.json`을 먼저 커밋/푸시.
  - 이후 맛집 단계 실패가 발생해도 초이스 글과 히스토리, git log 기반 리포트 근거가 보존되도록 순서 강화.
- 2026-04-13 핵심 반영(추가):
  - **텔레그램 리포트 확장**: `scripts/notify-telegram.mjs`에 초이스 건수/제목 목록과 fallback 완화 발동 횟수(적용 하한 포함) 노출을 추가.
  - 과거 JSON(초이스 필드 미포함)도 0건으로 안전 처리하도록 호환성 유지.
- 2026-04-13 핵심 반영(추가):
  - **빌드 경고 해소**: `src/lib/posts.ts` 파일 스캔 경로를 고정형으로 정리해 Turbopack의 광범위 패턴 경고 제거.
  - **일일 리포트 통계 강화**: `scripts/write-daily-report.mjs`에서 `published_by(auto/manual/unknown)`를 전체/블로그/초이스/맛집 단위로 집계.
  - **fallback 가시화**: 초이스 생성기 output(`applied_min_rating`, `relaxed_fallback_applied_count`)을 워크플로우에서 리포트로 전달해 발동 횟수와 적용 하한을 노출.
- 2026-04-13 핵심 반영(추가):
  - **초이스 fallback 2단계 완화**: 대체 키워드까지 소진해도 3개를 못 채우면 평점 기준을 `4.5 -> 4.3 -> 4.0` 순으로 완화해 재평가.
  - **품질 하한 유지**: fallback 완화 시에도 `reviewCount >= 100`, 품절 제외, 최근 14일 중복 제외 기준은 유지.
  - **published_by 확장**: 초이스뿐 아니라 일반 블로그 자동 생성본과 맛집 자동 생성본에도 frontmatter `published_by`를 기록하도록 확장.
- 2026-04-13 핵심 반영(추가):
  - **초이스 테마 외부화**: `scripts/generate-choice-posts-auto.js`의 요일별 테마 설정을 `scripts/data/choice-daily-themes.json`로 분리.
  - **히스토리 발행구분 추가**: `scripts/data/recommended-products.json` 엔트리에 `publishedBy(auto/manual)` 저장.
  - **중복 정책 분리 지원**: 기본은 global 중복 방지이며, `CHOICE_DEDUP_SCOPE=same-publisher`로 자동/수동 발행 이력을 분리 운영 가능.
  - **fallback 키워드 확장**: 1차 키워드 후보가 부족하면 테마 파일의 `fallbackKeywordHint`까지 자동 검색 확장.
  - **기존 설정 파일 정리**: 미사용 `scripts/choice-auto-topics.json` 제거.
- 2026-04-13 핵심 반영(추가):
  - **초이스 무인 엔진 고도화**: `scripts/generate-choice-posts-auto.js`를 KST 요일 기반 7대 테마 선택 방식으로 전환(월 건강/화 생활/수 주방/목 디지털/금 반려 생활/토 뷰티·패션/일 가전·가구).
  - **스마트 상품 선정 강화**: `scripts/generate-choice-post.js`에서 `sort=bestAsc` 상위권 수집 후 최근 14일 `productId` 중복 제외 + 품질 필터(`rating>=4.5`, `reviewCount>=100`, `outOfStock=false`) + 브랜드 다양성(최소 2브랜드) 적용.
  - **히스토리 기록 추가**: 발행 완료 시 사용된 3개 상품의 `productId`/날짜/포스트 파일을 `scripts/data/recommended-products.json`에 즉시 기록.
  - **운영 규칙 동기화**: `.github/copilot-instructions.md`에 초이스 무인 엔진 규칙(요일 테마/중복 방지/품질 필터/브랜드 다양성/기록) 추가.
- 2026-04-13 핵심 반영(추가):
  - **초이스 훅/소제목 다양성 규칙 반영**: `scripts/generate-choice-post.js`에 4개 글쓰기 앵글(문제 해결형/트렌드 중심형/전문가 큐레이션/가성비·효율 강조) 랜덤 선택 로직 추가.
  - **자동/반자동 공통 적용**: `generate-choice-posts-auto.js`가 동일 생성기를 호출하므로 자동/반자동 모두 동일 다양성 규칙이 적용됨.
  - **소제목 스타일 강화**: 번호형 라벨(`1. 장점`, `2. 특징`) 금지 + 질문형/감탄형 소제목 혼합 규칙을 프롬프트에 통합.
  - **운영 문서 동기화**: `.github/copilot-instructions.md`에 12번 `초이스 훅/소제목 다양성 규칙` 추가.
- 2026-04-13 핵심 반영(추가):
  - **초이스 지침 정합화**: `.github/copilot-instructions.md`의 10번 문구를 "본문 배너/위젯 금지 + 텍스트 CTA 링크 허용"으로 정정해 11번 수익 링크 정책과 충돌 해소.
  - **초이스 날짜 KST 통일**: `scripts/generate-choice-post.js`의 `todayIso()`를 KST(UTC+9) 기준으로 변경.
  - **프롬프트 중복 라인 정리**: 본문 구조 지시문 중 동일 문장 1줄 제거.
  - **환경변수 로더 정리**: `scripts/lib/coupang-api.js`에서 `.env` 로드를 제거하고 `.env.local`만 읽도록 변경.
  - **환경 상태 확인**: `.env` 없음, `.env.local` 존재, `COUPANG_ACCESS_KEY`/`COUPANG_SECRET_KEY` 등록 확인(값 미노출).
- 2026-04-13 핵심 반영(추가):
  - **life 목록 썸네일 누락 수정**: `src/app/life/page.tsx`의 초이스 카드 매핑을 `c.image || c.coupangBannerImage`로 보강.
  - `src/lib/life-choice.ts`에서 `coupangBannerImage`를 `ChoiceArticle`에 포함해 목록 카드 fallback 경로를 확보.
- 2026-04-13 핵심 반영(추가):
  - **초이스 목록 썸네일 fallback 보강**: `src/components/BlogFilter.tsx`에서 초이스 카드 썸네일을 `post.image` 우선, 없으면 `post.coupangBannerImage`로 표시.
  - 멀티상품 포스트의 히어로 비활성(`image: ""`)과 목록 썸네일 표시를 분리해 기본 썸네일 회귀 방지.
- 2026-04-13 핵심 반영(추가):
  - **초이스 본문 소제목 간격 조정**: `src/app/globals.css`의 `choice-post-prose h3` 여백을 축소해 번호형 소제목 간격 개선.
  - **사이드바 배너 간격 조정**: `src/app/blog/[slug]/page.tsx`에서 태허철학관 배너 하단 간격을 확대해 첫 상품 배너와 시각적 분리 강화.
- 2026-04-13 핵심 반영(추가):
  - **사이드배너 1개 회귀 원인 복구**: `src/app/blog/[slug]/page.tsx` 링크 파서의 캡처 인덱스를 `match[1]`(텍스트)에서 `match[2]`(URL)로 수정해 다중 배너 추출 복원.
- 2026-04-13 핵심 반영(추가):
  - **프로바이오틱스 사이드배너 누락 보정**: escaped 링크 텍스트 파싱 정규식 보강으로 제품 3개 모두 사이드배너 노출.
  - **배너 제목 가독성 개선**: 번호형 라벨 대신 제품명 표시, 제목 폰트 크기/굵기 상향.
- 2026-04-13 핵심 반영(추가):
  - **프로바이오틱스 포스트 보정**: `2026-04-13-choice-probiotics-api-curation.md`의 하단 2개 상품을 GFM 가로 비교 테이블로 수정하고 `오늘의 추천 장비` 문구 제거.
  - **사이드바 제품 파서 보강**: `src/app/blog/[slug]/page.tsx`에서 escaped alt 지원 및 `link.coupang.com/re/` CTA 링크만 매칭하도록 개선.
  - **초이스 생성기 재발 방지**: `scripts/generate-choice-post.js`에서 legacy 세로 상품 블록/헤딩 정리 후 표준 Pick+비교 섹션을 주입.
- 2026-04-13 핵심 반영(추가):
  - **초이스 섹션 제목 정책 보정**: 메인 1번 상품은 `오늘의 픽 (Pick of the Day)` 유지, 하단 2개 비교 섹션에만 4종 랜덤 제목 사용.
  - "오늘의 추천 장비 → 오늘의 추천 상품" 일괄 치환은 적용하지 않음.
- 2026-04-13 핵심 반영(추가):
  - **초이스 중복 고지문 제거**: 생성기(`scripts/generate-choice-post.js`)에서 본문 말미 고지문/구분선 자동 추가를 중단하고 기존 중복 문구 정리만 수행.
  - **초이스 히어로 노출 보정**: `src/app/blog/[slug]/page.tsx`에서 본문 상품 블록(2개 이상) 감지 시 히어로를 숨기고 본문 첫 이미지 유지.
  - **비교 섹션 개선**: 하단 비교 섹션 제목 4종 랜덤 + 비교/다양성 연결 문장 반영.
  - **생성기 문법 안정화**: `scripts/generate-choice-post.js` 함수 중첩 오류로 인한 `Unexpected end of input` 복구.
- 2026-04-13 핵심 반영(추가):
  - **초이스 상세 사이드바 다중 배너**: `src/app/blog/[slug]/page.tsx`에서 초이스 본문의 이미지/쿠팡 링크를 추출해 사이드바에 순서대로 모두 렌더링하도록 변경.
  - 단일 배너 frontmatter(`coupang_link`, `coupang_banner_image`)는 fallback으로 유지.
- 2026-04-14 핵심 반영:
  - **초이스 레이아웃 4대 개선**: `scripts/generate-choice-post.js`에서 멀티상품 히어로 제거, Pick of the Day(1번 상품) 블록, 2/3번 비교 테이블(GFM), 자연 서론 지침을 반영.
  - **렌더/SEO 보강**: `src/app/blog/[slug]/page.tsx`의 OG 이미지를 `post.image || post.coupangBannerImage`로 fallback 처리.
  - **이미지 크기 최적화**: `src/app/globals.css`의 `.choice-post-prose` 규칙으로 본문 이미지(220px)와 테이블 이미지(160px) 크기 제한 적용.
  - **초이스 카드 동일 적용**: `src/components/life/ChoiceArticleCard.tsx`에도 `choice-post-prose` 클래스 적용.
- 2026-04-13 핵심 반영:
  - **쿠팡 API 초이스 자동화 1차 구현**: `scripts/lib/coupang-api.js` 신규 추가. 쿠팡 Search API HMAC 서명과 상품 정규화 구현.
  - **초이스 생성기 자동화 확장**: `scripts/generate-choice-post.js`에 `keywordHint` 입력, 상품 3개 자동 검색, 상단/중간 이미지+CTA 주입, fallback, 품질 검증 및 재시도 로직 추가.
  - **자동화 안정성 보강**: 금지 표현 자동 치환 레이어 및 키워드 신호 가중치 기반 상품 관련도 랭킹을 추가해 자동 생성 실패율을 낮춤.
  - **워크플로우 통합 완료**: `.github/workflows/deploy.yml`의 full 모드에 `[2.5단계] 픽앤조이 초이스 자동 생성` 단계 추가, `scripts/generate-choice-posts-auto.js` + `scripts/choice-auto-topics.json` 구성.
  - **리포트 확장**: `scripts/write-daily-report.mjs`에 초이스 생성 단계 상태(`generate_choice`)와 초이스/맛집 생성 파일 분리 집계 반영.
  - **API 샘플 발행 검증**: `scripts/choice-input.probiotics-api.json`로 자동 생성 성공, 결과 파일 `src/content/life/2026-04-13-choice-probiotics-api-curation.md`, `npm run build` 성공.
  - **festival.json 만료 자동화**: `scripts/cleanup-expired.js`에 festival.json 패스 추가. KST 기준 `eventenddate < today` 항목 `expired: true` 자동 마킹. 첫 실행 시 47건 처리.
  - **블로그 리드타임 KST 보정**: `scripts/generate-blog-post.js`에 `getTodayKST()` 헬퍼 및 `BLOG_FESTIVAL_MIN_DAYS_BEFORE_END`(기본 7일) 적용. 종료 7일 이내 축제는 글 생성 대상에서 제외.
  - **맛집 버킷별 재시도**: `scripts/generate-life-restaurant-posts.mjs`에 `backupsByBucket` Map 도입. 주 후보 실패 시 같은 버킷 대체 후보로 자동 재시도.
  - **Gemini STOP 오탐 수정**: `finishReason=STOP` 응답이 쿠팡 링크/마크다운 끝 문자(`)`/`*`) 때문에 불완전 판단 오탐되던 버그 수정. STOP일 때는 길이(700자 이상)·FILENAME 여부만 체크.
  - **서울 맛집 포스트 수동 생성**: `src/content/life/2026-04-13-seongsu-restaurant-804984272.md` (브루잉세레모니, 서울 성수)
- 2026-04-12 핵심 반영:
  - **픽앤조이 초이스 신규 발행**: `src/content/life/2026-04-12-choice-lapomme-dual-coretex-airhole-pouch-6pack.md` 생성
  - **이미지 자산 반영**: `public/images/choice/dual-core-hero.png`, `public/images/choice/dual-core-detail.png`
  - **생성 입력 스냅샷 추가**: `scripts/choice-input.lapomme-dual-coretex.json`
  - **본문 규칙 보정**: 생성본 CTA 링크 누락을 수정하고, 중간 상세 이미지 1장 + 제휴 링크 1회로 고정
  - **배포 파이프라인 반영**: build 성공 후 커밋/푸시 완료, `git pull --rebase`로 원격 선행 커밋 정합화
- 2026-04-11 핵심 반영:
  - **후속 안정화(추가 반영)**:
    - 마크다운 본문 링크/렌더링 보정: `바로가기` 링크 자동 줄바꿈 정규화, 숫자 목록 하위 불릿 코드블록 오인식 보정(`src/lib/markdown-utils.ts`)
    - 맛집 3권역 균형 보장: `unused 총량`뿐 아니라 `버킷 누락`도 재수집 조건으로 확장(`scripts/generate-life-restaurant-posts.mjs`, `scripts/ensure-life-restaurant-candidates.mjs`)
    - 축제/행사/여행 정보 섹션 자동 분기: 콘텐츠 키워드 기준으로 `한눈에 보는 축제/행사/여행 정보` 헤딩 + 표 라벨 동적 전환(`scripts/generate-blog-post.js`)
    - 벚꽃 배경 하이브리드: 데스크톱 fixed / 모바일 scroll 전환 및 fallback 배경색 적용(`src/app/globals.css`)
    - 본문 소제목 간격 전역 개선: `blog-prose` + `prose`에서 h2/h3 상단 여백 확대, 예외는 첫 훅(h2)만 유지(첫 h3 예외 제거)
  - **Choice 구조화 데이터 수정**: `src/app/blog/[slug]/page.tsx`에서 Choice 글 보조 JSON-LD를 `Product` 단독 스키마에서 `Review + itemReviewed(Product)` 구조로 교체
  - **판매자 목록 오류 차단**: `offers` 제거로 Search Console의 `price`, `shippingDetails`, `hasMerchantReturnPolicy` 요구 차단
  - **작성자 엔티티 정리**: 구조화 데이터의 `author`는 개인 실명 대신 `Pick-n-Joy Editor`(`Person`)를 사용하고 `publisher`는 `픽앤조이`(`Organization`)로 유지
  - **제품 리뷰 문맥 강화**: `reviewRating`, `author`, `publisher`, `reviewBody`, `aggregateRating`를 포함해 리뷰 페이지로 해석되도록 조정
- 2026-04-10 핵심 반영:
  - **HOOK/첫 소제목 레이아웃 고정**: `scripts/generate-life-restaurant-posts.mjs` 후처리에 HOOK 다음 공백 1줄, 브릿지 문단 2~3줄, 소제목 위·아래 2줄 여백 강제 로직 추가
  - **첫인상 구조 Validator 강화**: HOOK 직후 여백 누락/브릿지 미달/첫 소제목 전 서론 과다(150자 초과)/`###` 여백 미충족 검출을 추가해 인스타 매거진형 첫인상 레이아웃 유지
  - **가독성 검증 정밀 조정**: 소제목 하위 문단 과장 길이 및 소제목 없는 긴 줄글 연속 검출을 문맥 기반으로 보정
  - **맛집 생성 호출 구조 개편**: `scripts/generate-life-restaurant-posts.mjs`를 후보 1건 단위 독립 처리로 정리하고, 후보별 `try-catch`를 적용해 특정 글 생성 실패 시에도 다음 후보 생성을 계속 진행하도록 보강
  - **API 과부하 완화 지연 제어**: 호출 간 대기 시간을 `INTER_REQUEST_DELAY_MS`(기본 1000ms) 환경변수로 분리
  - **실행 요약 로그 추가**: 실행 종료 시 `성공/실패 건수`를 출력하도록 개선
  - **맛집 본문 소제목 구조 복구**: `scripts/generate-life-restaurant-posts.mjs` 프롬프트에 소제목 최소 3~4개 + `###`/`**` 형식 강제 및 문장형 예시 레이아웃 명시
  - **후처리 검증 강화**: 소제목 개수/가독성(긴 줄글 연속) 체크 추가, `**상호명**:` 형태를 허용하도록 필수 항목 검증 정규식 보강
  - **JSON 응답 자동 복원**: Lite가 JSON 포맷으로 응답해도 frontmatter+본문 마크다운으로 자동 변환 저장
  - **검증 오탐 보정**: 소수점(예: 4.2) 기준 문장 분리 오탐을 줄여 평어체 검증 정확도 개선
  - **맛집 생성 지침 Lite 최적화**: `scripts/generate-life-restaurant-posts.mjs`에서 temperature를 `0.7`로 하향하고 프롬프트를 `[필수]/[스타일]/[금지]` 구조로 개편, 본문 전개를 `HOOK -> SCENARIO -> SENSORY -> TRANSITION`으로 고정
  - **Lite 완결성 판정 보정**: `looksIncompleteGeminiOutput` 최소 길이 기준을 `900 -> 700`으로 조정해 정상 완성본의 오탐 실패를 완화
  - **동일 후보 재생성 검증**: `FORCE_RESTAURANT_SOURCE_IDS=26941015,145791269,25081012`로 어니언 성수/스이또스이또/세렌 재생성 완료
  - **블로그 상세 하단 간격 보정(인천/보조금 한정)**: `src/app/blog/[slug]/page.tsx`에서 인천/보조금 카테고리 글에만 `공식 원문 바로가기` 버튼 영역을 `flex flex-col gap-8 + my-12`로 확장해 버튼과 AI 안내 문구 사이 시각적 여백 확보
  - **만료 보조금 자동 감지 보강**: `scripts/collect-subsidy.js`와 `scripts/cleanup-expired.js`에 `(YYYY.MM.DD.한)` 패턴 파싱 로직을 추가해 `지원내용/지원대상/선정기준`에 기한이 지난 항목은 자동으로 `expired: true` 처리
  - **만료 보조금 포스트 정리**: `서비스ID 131200000013`(사회적기업 지방세 감면)을 `expired: true`로 수정하고 연관 포스트(`2026-04-09-social-enterprise-tax-reduction.md`) 삭제
  - **맛집 생성 모델 안정화**: `scripts/generate-life-restaurant-posts.mjs`의 Gemini 호출 모델을 환경변수 `GEMINI_MODEL` 기반으로 전환(기본 `gemini-2.5-flash-lite`), deploy 3단계 수집/생성 env 모두 `gemini-2.5-flash-lite`로 고정
  - **재발 방지 가드**: 핵심 생성 스크립트에 `ALLOW_GEMINI_PRO` 안전장치 적용(명시 허용 없이는 Pro 모델 차단)
  - **모델 참조 일원화 확장**: 보조 재작성/테스트 스크립트와 `src/lib/life-restaurants.ts`까지 env 기반 모델 참조로 통일
  - **Gemini 모델 티어 정책 문서화**: `collect-life-restaurants.mjs`의 `RESTAURANT_GEMINI_MODEL_FALLBACK = 'gemini-1.5-flash'`는 수집(후보 필터링) 전용이며 글 생성 없음 → 의도적 구분. `copilot-instructions.md`에 정책 섹션 추가해 향후 실수 통일 방지
  - **Cloudflare 캐시 자동 퍼지 완성**: `CLOUDFLARE_API_TOKEN` 권한에 `Zone > Cache Purge > Purge` 추가 완료. `deploy.yml`의 5개 배포 단계 모두에 `purge_everything` curl step 추가 → 배포 후 항상 최신 콘텐츠 노출
  - **GitHub Secret 추가**: `CLOUDFLARE_ZONE_ID` (`b1674a4a2e034dc03f96d451cc95df7d`)
  - **`.env.local`에 Zone ID 추가**: 로컬 수동 퍼지 명령어용
- 2026-04-08 핵심 반영:
  - **맛집 재수집 시점 수정**: GitHub Actions 04:00 full 실행 시에도 `collect-life-restaurants.mjs`를 매일 무조건 돌리지 않고, `ensure-life-restaurant-candidates.mjs`가 기존 `restaurants.json`의 unused 후보 수를 점검해 10건 이상이면 생략, 10건 미만일 때만 재수집
  - **맛집 DB 직결 전환 기준 고정**: `restaurants_cache` 1,000건 이상, `restaurants.json` 성능 이슈, 동적 기능 요구(즐겨찾기/조회수) 충족 전에는 JSON 스냅샷 기반 발행 구조 유지
  - **절감 KPI 가시화**: 맛집 수집 단계에서 `cache_hit`, `cache_miss`, `google_called`를 집계해 일일 리포트/텔레그램에 노출
  - **Supabase 평점 캐시 적용**: `scripts/collect-life-restaurants.mjs`에서 `restaurants_cache(kakao_id)` 조회 후 캐시 hit면 Google Places 호출을 생략하고, miss일 때만 조회 후 `upsert`
  - **환경변수 운영 통일**: 로컬 민감키는 `.env.local`만 사용하고 `.env`는 유지하지 않도록 가이드 반영
  - **맛집 수집 비용 최적화**: `scripts/collect-life-restaurants.mjs`에서 요약 모델을 환경변수화(`RESTAURANT_GEMINI_MODEL` -> `GEMINI_MODEL` -> `gemini-1.5-flash`)하고, Google Places FieldMask를 `id/rating/userRatingCount` 최소 필드로 축소
  - **관리자 로그 링크 로그인 경유 처리**: `src/app/admin/page.tsx`, `src/app/admin/runs/page.tsx`의 GitHub Actions 링크를 `github.com/login?return_to=...`로 통일하고, 공통 유틸 `src/lib/github-auth-link.ts` 추가
  - **관리자 보안 구조 확인**: 현재 배포는 정적 export라 Next.js `middleware.ts` 기반 서버 인증은 적용 불가. `/admin` 보안은 Cloudflare Access 경로 정책 정합성(`/admin`, `/admin/`, `/admin/*`)으로 관리해야 함
  - **인천/보조금/축제 건수 표시**: 목록 3개 페이지 헤더에 `총 N건` 카운트 추가, 헤더-카드 간격 `mb-8 → mb-12` 확대
  - **네비 설명 문구 세련화**: `incheon/subsidy/festival/life/blog` 목록 페이지 설명을 혜택 중심 문장으로 교체하고, 제목-설명 간격(`mt-3`) 및 설명색(`text-gray-500`)을 통일
  - **상단 여백 배경 복구**: `src/components/PageContentShell.tsx`에 `bg-cherry-blossom`을 추가해 비홈 페이지 네비 아래 여백의 흰 배경 노출 문제 해결
  - **공통 상단 여백 정리(홈 제외)**: `src/components/PageContentShell.tsx` 신설 + `src/app/layout.tsx` 연결로 비홈 페이지의 네비게이션 아래 숨 공간을 `pt-8 md:pt-10`으로 일괄 확대
  - **일상의 즐거움 탭 라벨 명확화**: `src/components/LifeFilterClient.tsx`의 `맛집 탐방` 탭을 `서울·인천·경기 맛집 탐방`으로 변경
  - **인천 페이지 헤더 미세 보정**: `src/app/incheon/page.tsx`에서 아이콘-제목 간격 `gap-2 -> gap-3` 조정
  - **맛집 파이프라인 고도화**: Google Places 필드 확장(가격/영업상태/주 업종/영업시간), `CLOSED_PERMANENTLY` 수집 단계 자동 제외, 가격 힌트 완전 문장화, 미완성 요약 자동 탐지, vibePhrase 중복 제거 → `인천 30 / 서울 30 / 경기 28건`
  - **일상의 즐거움 카드 제한 해제**: `src/app/life/page.tsx`의 초이스(6)·맛집(12) 하드캡 제거, `src/lib/life-choice.ts` 기본 limit `1000` 상향
  - **신뢰도/정책 보강**: `src/app/about/page.tsx` 6개 섹션 확장 + AboutPage JSON-LD, `src/app/layout.tsx` 전역 설명/스키마(WebSite/Organization) 강화
  - **이용약관 페이지 신설**: `src/app/terms/page.tsx` 추가(면책·저작권·문의 포함)
  - **푸터 골든 트리오+문의 완성**: `소개 | 개인정보 처리방침 | 이용약관 | 문의하기(mailto)` 반영
  - **푸터 가독성 통일**: 보조 문구 색상 `text-gray-400`으로 통일
  - **네이버 URL 검사 대응**: `src/app/page.tsx` 홈 `description`/`openGraph.description` 80자 이내 권장 길이로 축약
  - **네이버 크롤링 보강**: `scripts/generate-sitemap.js`에 `/privacy/`, `/terms/`를 추가해 sitemap 탐색 범위 확장
  - **홈 히어로 통계 정렬 보정**: 하단 3개 지표에서 중앙 고정 + 좌우 간격 확장으로 CTA 버튼 하단 정렬감 개선
  - **최종 미세 조정**: 통계 영역 폭을 CTA 버튼 묶음 폭에 맞게 다시 축소해 좌우 벌어짐 완화
  - **CTA 버튼 폭 통일**: 홈 상단 3개 CTA를 동일 폭 grid로 재구성하고, 문구를 `이번 주말 전국 행사·축제`, `요즘 뜨는 인천·서울·경기 맛집`으로 조정
  - **최종 정렬 보정**: CTA 내부 아이콘/텍스트 구조 통일, 좌우 통계를 각 카드 중심축 아래로 재배치, 히어로 `overflow-hidden`으로 내부 스크롤 제거
  - **여백 보정**: 설명 문단 하단 여백을 `mb-16 md:mb-20`으로 늘려 CTA와의 간격 답답함 완화
  - **Problem 카드 정리**: 3개 카드를 중앙 정렬 구조로 통일하고, 1번 문구를 `수많은 정보 속 나에게 딱 맞는 혜택은?`으로 축약
  - **최종 단순화**: 숫자 배지(1·2·3) 제거, 텍스트 크기 확대 후 두 줄 메시지 카드 중심으로 재구성
  - **섹션 분리 여백 강화**: Problem 섹션 하단과 서비스 소개 섹션 상단 간격을 확장(`pb-8`, `pt-16 md:pt-20`)해 "문제 제기 -> 해결책" 단계 전환을 더 명확하게 표현
  - **챕터 전환 장치 추가**: Category 섹션 상단에 옅은 1px 그라디언트 페이드 라인을 추가해 여백 구간의 목적성을 강화
  - **푸터 전 여백 강화**: Features+CTA 구간 하단 여백을 `pb-16 sm:pb-20`으로 확대하고, `SiteFooter` 상단에 `border-t border-white/10` + `pt-8`을 적용해 마지막 핵심 문구와 부속 정보 영역의 분리감을 향상
  - **문서 lint 기준선 정리**: `WORK_LOG.md`의 강조문 헤딩(`**...**`)을 실제 헤딩(`####`)으로 교체하고 리스트 전후 공백을 정리해 markdownlint 경고(MD036/MD032) 제거
  - **홈 카드 선명도 개선**: 카테고리 헤더 detail 라인과 카드 본문/출처 텍스트 대비를 상향하고, 마지막 무료 안내 문구는 모바일에서만 자연 줄바꿈되도록 `whitespace-normal sm:whitespace-nowrap`로 조정
  - **맛집 탐방 상세 연결 강화**: `src/app/life/restaurant/page.tsx`에서 맛집 카드와 맛집 블로그 포스트를 `source_id`/`place_name` 기준으로 매칭해, `RestaurantExplorer` 카드에 `해당 블로그 내용 보기` 링크를 조건부 노출
  - **맛집 링크 노출 기준 확인**: `restaurants.json` 전체 89개 중 블로그 매칭은 26개만 존재해, 맛집 카드의 블로그 링크는 전면 노출이 아니라 조건부 유지가 맞음
  - **맛집 요약 문구 보정**: `src/lib/life-restaurants.ts`에 generic summary 감지 + 상황형 요약 생성 로직을 추가해, 스냅샷에 저장된 반복 문장도 런타임에서 더 자연스러운 카드 카피로 치환
- 2026-04-07 핵심 반영:
  - **픽앤조이 초이스 수동 포스트 추가**: Panasonic ES-148 눈썹 바리깡 포스트 신규 작성
  - **픽앤조이 초이스 수동 포스트 추가**: 톰보 모노 제로 샤프형 지우개 포스트 신규 작성
  - **이미지 자산 반영**: 사용자 제공 이미지 2종을 `public/images/choice/`로 이동 후 본문/대표 이미지에 연결
  - **수익 링크 메타 반영**: `coupang_link`, `coupang_banner_image`, `coupang_banner_alt` frontmatter 설정
  - **재발 방지 보완**: 초이스 상세에서 `frontmatter.image`와 본문 첫 이미지 중복 시 자동 제거 가드 적용
  - **CTA 중복 방지 보완**: 생성 지침을 "중간 1회"로 고정하고, 동일 쿠팡 링크 다중 삽입 시 후처리로 1회만 남기도록 적용
- 2026-04-04 핵심 반영:
  - **자동화 개선 완료**:
    - 데이터 수집 검증(벨리데이션 경고): API 0건 또는 50% 미만 시 경고
    - Anthropic 사용량 추적: GitHub Actions output + 일일 리포트에 반영
    - Prompt 안정화: Generate 함수의 temperature 조정 (0.9 → 0.4/0.35) + topP 추가
  - **문서 규칙 자동화 변경**: 커밋/배포 후 사용자 확인 질문 제거, 기계적 동기화로 루프 방지
  - **검증**: npm run build 성공, 모든 스크립트 신택스 오케이
- 2026-04-03 핵심 반영:
  - **GitHub Actions 스케줄 04:00 KST로 변경** (`0 19 * * *`)
  - **admin/admin/runs 배경 bg-cherry-blossom 통일** (gradient → 꽃무늬 배경)  - **홈페이지 히어로 리디자인**: 진한 orange 그라데이션 → 흰색→오렌지 소프트 그라데이션 + 어두운 텍스트 (눈 피로 감소)
  - **콘텐츠 배경 체계 정리**:
    - `bg-cherry-blossom`: 페이지 전체 배경 → 원본 벚꽃 이미지 유지
    - `bg-content-floral`: 메인 콘텐츠 컨테이너 → 새 개나리 꽃 이미지 (`bg-content.png`)
    - 적용 9곳: 상세 4개(incheon/subsidy/festival/blog) + about + privacy + life/choice(2곳) + life/restaurant
  - **홈페이지 히어로 아래 섹션 흰색**: 웨이브 아래 `bg-cherry-blossom` → `bg-white`, SVG fill `#ffffff`
  - **하단 Features 섹션 주황색 유지**: `from-orange-50 to-orange-100` 복원 (사용자 요청)
  - **Sticky 사이드바 GPU 강화**: `translate3d(0,0,0)` + `will-change: transform, top` + `perspective: 1000px` + `contain: layout style` + `transition: none` + `top: 5.95rem` (서브픽셀)
  - **홈 배경 아이보리 크림**: `bg-white` → `bg-[#F9F9F7]`, SVG fill 동기화
  - **CTA 버튼 통일**: 3개 모두 `bg-white text-orange-600 border border-orange-200`
  - **카테고리 카드 UI 개선**: 헤더 중앙정렬, 카드 화살표 카테고리별 색상(blue/orange/purple-600), 더보기 `font-bold` + 카테고리 색상 + 점선 제거, strokeWidth `2.5`
  - **맛집 파이프라인 3지역 분리**: 기존 2지역(`incheon-gyeongin`, `seoul-gyeonggi`) → 3지역(`incheon`, `seoul`, `gyeonggi`)
    - collect: 지역당 8개 검색어, MAX_ITEMS_PER_REGION 30, GOOGLE_PRE_FILTER_SIZE 50
    - generate: `gyeonggi-other` → `gyeonggi` 통일, 3지역 regionLabel/areaTag 매핑
    - frontend: `LifeRegionTab` 3지역 타입, RestaurantExplorer 3탭, restaurant/page.tsx 3지역 로드
    - 수집 결과: 인천 30, 서울 30, 경기 30 (총 90개 후보), 월 1회 수집으로 충분
  - **맛집 후보 자동 재수집**: 빈 버킷 감지 시 `collect-life-restaurants.mjs` 자동 호출
  - **텔레그램 리포트 4건 수정**: 경기 포스트 미생성 해결, 테이블 CSS nowrap, deploy.yml 리포트 후 재배포
- 2026-04-02 핵심 반영:
  - **한글 파일명 인코딩 버그 수정 (write-daily-report.mjs)**:
    - `git log`에 `-c core.quotePath=false` 추가 → 한글 블로그/맛집 파일명 정상 인식
    - 근본 원인: Git 기본 `core.quotePath=true`가 한글을 octal 이스케이프 → 패턴 매칭 실패
    - 이 수정으로 앞으로 모든 일일 리포트에서 정확한 블로그/맛집 카운트 보장
  - **텔레그램 알림 데이터 수집 결과 추가**:
    - 수집 스크립트 3개에 `GITHUB_OUTPUT` collect_summary 출력 추가
    - deploy.yml → write-daily-report.mjs → notify-telegram.mjs 데이터 흐름 구축
    - 3개 신규 라인: 📡 수집 단계 아이콘, 📋 항목별 건수, 📂 변경 파일
  - **맛집 포스트 제목 규칙 개선**: 음식 장르/특색 필수 포함 (기존: 상황 위주)
  - **admin/runs 4/2 데이터 보정**: generatedBlogPosts 1→3건, 한글 파일명 복원
  - **초이스 포스트 본문 배너 금지 규칙**: 3편 일괄 제거 + 생성기/copilot 규칙 명문화
  - **종료일 지난 항목 블로그 생성 방지**: `isEndDatePassed()` 날짜 필터 추가 (축제 YYYYMMDD / 인천·보조금 YYYY-MM-DD). `expired` 플래그만으로 부족한 시점 차이 해결
- 2026-04-01 핵심 반영:
  - **Cloudflare Worker `pick-n-joy-trigger` 배포 완료**:
    - URL: `https://pick-n-joy-trigger.royshong01.workers.dev`
    - 코드: `workers/trigger/index.js` + `workers/trigger/wrangler.toml`
    - Secrets: `ADMIN_SECRET`, `GITHUB_TOKEN` (wrangler secret put 설정)
    - `/admin/runs/` 수동 트리거 버튼 정상 작동 확인
  - **Admin 페이지 UI 수정**:
    - admin/runs + admin 메인 상단 여백 `pt-20 pb-10` 적용
    - admin 메인 "수동 실행 트리거" 플레이스홀더 카드 제거
  - **블로그/맛집 카테고리 썸네일 이미지 교체**:
    - 블로그: 인천(`incheon-thumbnail.jpg`), 보조금(`subsidy-thumbnail.png`) 커스텀 일러스트 적용
    - 맛집: 인천/서울/경기 3개 지역 모두 커스텀 일러스트 적용
    - `BlogFilter.tsx`의 `CATEGORY_THUMBNAIL_IMAGES`, `LifeFilterClient.tsx`의 `RESTAURANT_THUMBNAIL_IMAGES` 매핑 구조
    - 이미지 없는 카테고리는 기존 그라데이션 fallback 유지
  - **정책/소개 페이지 UI 마감 보정**:
    - `src/app/privacy/page.tsx` 상단 설명문과 1번 섹션 간격 확대
    - `src/app/about/page.tsx` 우측 사이드 배너(태허/쿠팡) 제거
  - **문의 이메일 프로젝트 계정으로 교체**:
    - `roysshong@gmail.com` → `royshong01@gmail.com`
    - 반영 위치: `about`, `privacy`
  - **Figma MCP 충돌 원인 정리**:
    - SethFord WebSocket 확장과 Copilot MCP 혼용 시 연결 실패
    - 충돌 확장 제거 후 `.vscode/mcp.json` 단일 경로 사용
    - `.gitignore`에 `.vscode/mcp.json` 유지(토큰 비커밋)
- 2026-03-31 핵심 반영:
  - **AdSense 검증 경로 안정화**:
    - `public/ads.txt` 배포 및 실서버 접근/크롤러 접근(200) 확인
    - `deploy.yml` 모든 Next.js build 단계에 `NEXT_PUBLIC_ADSENSE_ID='ca-pub-5984189992308575'` 주입
    - 원인 요약: 로컬 `.env.local`에는 값이 있었지만 CI build env에 값이 없어 배포 HTML에서 스크립트 누락 가능
  - **개인정보 처리방침 페이지 신설**: `src/app/privacy/page.tsx`
    - 쿠키 고지, DART 쿠키, 맞춤광고 거부 안내, 수집 정보/목적 포함
  - **Footer 심사 대응 링크 추가**: `src/components/SiteFooter.tsx`
    - `/privacy`, `/about` 링크 추가로 심사 봇 탐색성 강화
  - **서울 타깃 자동 생성 경로 추가**: `generate-blog-post.js`에 `BLOG_ONLY_KEYWORD` 필터 추가 (title/name/addr1/overview 기반)
  - **검증 완료**: `BLOG_ONLY_CATEGORY="전국 축제·여행"` + `BLOG_ONLY_KEYWORD="서울"`로 1건 자동 생성 성공 (`source_id: 2540520`)
  - **Gemini 키 로딩 복구**: `scripts/generate-choice-post.js`에서 `.env` 플레이스홀더 키가 `.env.local` 실키를 가리는 문제 수정 (`.env.local` override 우선)
  - **재검증 완료**: CJ 입력 파일로 자동 생성 성공 + 빌드 성공
  - **초이스 생성기 품질 보강**: `scripts/generate-choice-post.js`에 과장형 제목 금지, 비근거 수치/비교 결과 생성 금지, `outputFileName` 지원, `.env/.env.local` 자동 로드 추가
  - **CJ 바이오코어 포스트 재작성**: `src/content/life/2026-03-31-choice-cj-biocore-probiotics.md`를 캐츠랑 포스트와 같은 자연어형 초이스 톤으로 정리
  - **CJ 전용 입력 파일 추가**: `scripts/choice-input.cj-biocore.json`
  - **캐츠랑 20kg 초이스 포스트 추가**: `src/content/life/2026-03-31-choice-catsrang-20kg.md` 생성, 상단 전면 이미지와 본문 중간 후면 QR 이미지를 로컬 파일로 연결
  - **초이스 수동 생성 자동화 추가**: `scripts/generate-choice-post.js` 신설 + `scripts/choice-input.example.json` 추가 + `package.json`의 `generate:choice` 명령 등록
    - 현재는 API 연동 전 단계로, 수동 입력 JSON 기반으로 초이스 포스트를 즉시 생성 가능
    - 입력 필수값: `title`, `englishName`, `summary`, `coupangUrl`, `coupangHtml`
  - **sitemap 확장**: `scripts/generate-sitemap.js`를 전체 섹션/상세 페이지 포함 구조로 고도화 (`/incheon`, `/subsidy`, `/festival`, `/life`, `/life/choice`, `/life/restaurant`, `/rss.xml` 포함)
  - **사이트 목적 문구 SEO 반영**: `layout.tsx` 전역 metadata/OG/JSON-LD(WebSite+Organization), `page.tsx` 홈 metadata, `about/page.tsx` 운영 목적 문구 업데이트
  - **블로그 목록 정리**: 블로그 페이지에서 맛집 글 제외, 맛집 글은 `일상의 즐거움` 영역 중심으로 노출
  - **네비 UX 보정**: `일상의 즐거움` 메뉴를 `블로그` 앞에 배치, 검색 아이콘 크기 소폭 확대
  - **초이스 포스트 운영 보정**: CJ 바이오코어 포스트 추가 후 상단 이미지 로컬 교체, 본문 중복 이미지 제거, 단계형 헤딩 제거, 인코딩 복구
  - **초이스 Product JSON-LD 강화**: `aggregateRating`/`offers`를 frontmatter 값(`rating_value`, `review_count`, `coupang_link`)으로 자동 생성
  - **admin 수동 트리거**: `AdminTriggerPanel` 추가. `/admin/runs` 상단에서 Cloudflare Worker를 통해 GitHub Actions `deploy.yml`를 `full` 또는 `deploy_only` 모드로 즉시 실행 가능
  - **Cloudflare Worker 연동 방식 고정**: Worker URL `https://pick-n-joy-trigger.royshong01.workers.dev`, 헤더 `X-Admin-Secret`, 시크릿 `GITHUB_TOKEN` + `ADMIN_SECRET` 검증
  - **admin runs 상세 패널**: `RunsDetailPanel` 클라이언트 컴포넌트 신규 생성. 날짜 행 클릭 시 accordion으로 단계별 결과·커밋·파일 상세 표시. `[date]` 동적 라우트 방식은 `output: export` 제약으로 불가 → accordion 전환
  - **`getDailyRunReport(date)`**: `daily-runs.ts`에 단건 조회 함수 추가
  - **Gemini 비용 가드**: 모델 `gemini-2.5-flash-lite` 전환, 예산 500원 상한, maxOutputTokens 8192, 호출 간 5초 지연, API 호출 10회 캡
  - **admin 접근 보호**: Cloudflare Zero Trust OTP 인증, path `/admin/*`, 세션 24h
  - **Cloudflare Zero Trust UI 경로** (2025-11 이후): Login methods → `Integrations → Identity providers` / Access Apps → `Access controls → Applications`
  - **모바일 네비**: MobileNav 햄버거 메뉴 + 검색 버튼 추가 (`lg:hidden`, 드롭다운 5개 링크)
  - **블로그 보완**: 왕가의 산책 포스트 잘린 본문 완성 (source_id: 2433596)
  - **사이트 전체 검색**: Fuse.js 기반 퍼지 검색 (빌드타임 인덱스 377건, SearchOverlay + SearchButton + SiteHeader 통합)
  - **CTA 스타일 통일**: 보조금 CTA를 축제/맛집과 동일 glass-morphism 스타일로 변경 + 텍스트 축소(`whitespace-nowrap`) + 맛집 제목 1줄 처리
  - **히어로 맛집 CTA 추가**: 3번째 CTA `🍽️ 요즘 뜨는 맛집 보러가기` → `/life/restaurant` 추가
  - **히어로 상단 여백 축소**: `min-h-[85vh]` → `min-h-[70vh]` (네비↔배지 간격 35% 감소)
  - **글로벌 네비/푸터 적용**: SiteHeader 그라디언트 리디자인 + SiteFooter 신규 + layout.tsx 전역 반영
  - 10개 페이지 개별 헤더/푸터 제거 (중복 제거)
  - 소개("/about") 메뉴 네비/푸터에서 제거
  - 홈페이지 전면 개편: 태허 철학관 참고 디자인 기반 `page.tsx` 완전 재작성
  - copy.md 카피 적용 (Problem 섹션, CTA, 히어로 부제, 카테고리 상세)
  - Features + CTA를 주황색 그라데이션 단일 섹션으로 통합
  - 블로그 목록에서 초이스 포스트 필터링 + 썸네일 50% 축소
  - 깨진 블로그 글 6편 삭제 (3/28 Gemini API 인코딩 오류분)
  - 로고 이미지 적용 (홈 네비+푸터, `next/image` 컴포넌트)
  - 벚꽃 배경 이미지 적용 (홈 제외 전 페이지 `bg-cherry-blossom`)
  - 목록 페이지 카드 높이 통일 (`min-h-[220px]` → `h-full`, 블로그 방식)
  - copilot-instructions.md에 미승인 수정 금지 규칙 추가
- 이전(~03-30) 핵심:
  - 초이스 카테고리 신설 + 수동 리뷰 포스트 2편
  - 맛집 자동화 고도화 / 블로그·배포 안정화

## 고정 운영 사실 (반복 참조용)

- 쿠팡 파트너스: `AF5831775`
- 사이드바 배너: `976244` / 하단 배너: `976089`
- 쿠팡 iframe은 공식 URL 직접 사용 + `referrerPolicy="unsafe-url"` 유지
- `CoupangBanner.tsx`는 `'use client'` 유지 (Hydration mismatch 방지)

## Cloudflare Zero Trust 운영 참조

- `/admin/*` 보호: `pick-n-joy-admin` Application (Zero Trust → Access controls → Applications)
- OTP 설정 위치: Zero Trust → Integrations → Identity providers → One-time PIN
- 세션: 24시간, 로그인 이메일: 본인 계정
- 2025-11월 이후 UI 개편으로 구 경로(Settings → Authentication) 폐지

## Gemini API 비용 관리 (2026-03-31 기준)

- 모델: `gemini-2.5-flash-lite` (2.0-flash/1.5-flash는 이 계정에서 404)
- 일일 예산 상한: 500원 (`BLOG_DAILY_BUDGET_KRW`)
- maxOutputTokens: 8192 (`GEMINI_MAX_OUTPUT_TOKENS`)
- API 호출 캡: 10회 (`BLOG_MAX_API_CALLS`)
- 호출 간 최소 지연: 5000ms (`BLOG_GEMINI_MIN_DELAY_MS`)

## 유지 백로그 (요약)

- [x] GitHub Actions → Telegram 실행 결과 알림 (비용 + 생성 건수 + 데이터 수집 결과)
- [ ] Google AdSense 설정
- [x] Admin 수동 트리거 (Cloudflare Worker + GitHub Actions workflow_dispatch API)
- [ ] 맛집 포스트 이미지 소스 전략 고도화
- [x] posts.ts Turbopack 광범위 파일 접근 경고 (L169, L256) → `path.resolve` 리터럴 경로 + 단일 패스 리팩터로 해결 (2026-04-08)
