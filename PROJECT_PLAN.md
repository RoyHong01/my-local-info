# 프로젝트: pick-n-joy.com — 인천/전국 생활정보 수익형 웹사이트

> ⚠️ **이 문서는 2026-03 초기 기획서입니다. 아래 본문은 현재 구조와 다른 부분이 많습니다.**
> 현행 운영 실태·알려진 함정은 `PROJECT_CRITICAL_NOTES.md`,
> 작업 이력은 `WORK_LOG.md`를 참조하세요.
> 주요 차이: Next.js 16 / 실행 01:00 KST / Sonnet 5 Batch + Gemini 병용 /
> 맛집·초이스·큐레이션 모두 가동 중(기획서상 "추후"로 표기된 것들).
> **앞으로의 계획은 이 문서 맨 아래 "향후 로드맵"에 유지합니다.**

## 사이트 슬로건
"당신의 일상을 Pick, 당신의 주말을 Enjoy!"

## 목표
공공데이터포털 API, 한국관광공사 TourAPI, 카카오 로컬 API에서 정보를 자동 수집하고,
Claude AI가 매일 블로그 글을 자동 작성하며, Google AdSense + 쿠팡 파트너스로
수익화하는 독립 웹사이트. 기반 지역은 인천광역시.

## 기술 스택
- Next.js 14 (App Router) + TypeScript + Tailwind CSS
- Anthropic Claude API (블로그 글 자동 생성)
- 공공데이터포털 API, 한국관광공사 TourAPI, 카카오 로컬 API (데이터 수집)
- GitHub Actions (매일 07:00 KST 자동 실행)
- Cloudflare Pages (무료 호스팅, 도메인: pick-n-joy.com)

## 콘텐츠 카테고리 (4개)

### 카테고리 1 — 인천 지역 정보
- 소스: 공공데이터포털 API (행사정보, 축제정보, 문화행사)
- 대상: 인천광역시 내 행사, 축제, 지역 기획 프로그램, 지역 보조금
- 업데이트: 매일 07:00 자동 수집
- 만료 처리: endDate 기준 → expired: true 마킹, 목록에서 제외, 페이지 유지

### 카테고리 2 — 전국 보조금 · 복지 정책
- 소스: 공공데이터포털 API (복지 보조금, 정부 지원 정책)
- 대상: 청년지원, 출산지원, 주거지원, 노인복지 등 전국 단위 정책
- 업데이트: 매일 07:00 자동 수집
- 만료 처리: endDate 기준 → expired: true 마킹

### 카테고리 3 — 전국 축제 · 여행 정보
- 소스: 한국관광공사 TourAPI (국문 관광정보)
- 대상: 전국 축제, 관광지, 여행 추천, 계절 이벤트
- 업데이트: 매일 07:00 자동 수집
- 만료 처리: endDate 기준 → expired: true 마킹

### 카테고리 4 — 전국 맛집 정보 (2단계, 추후 구현)
- 소스: 카카오 로컬 API
- 대상: 지역별 맛집, 카페, 음식점 정보
- 업데이트: 주 1회 (상시 유효, 만료 없음)
- 상태: 현재 미구현 — 1~3 카테고리 안정화 후 추가

## 폴더 구조 (목표)

```
my-local-info/
├── src/
│   ├── app/
│   │   ├── layout.tsx                  # 루트 레이아웃 (AdSense, GA 포함)
│   │   ├── page.tsx                    # 메인 페이지
│   │   ├── incheon/                    # 카테고리 1 — 인천 지역 정보
│   │   │   ├── page.tsx                # 목록 페이지
│   │   │   └── [slug]/page.tsx         # 상세 페이지
│   │   ├── subsidy/                    # 카테고리 2 — 전국 보조금·복지 정책
│   │   │   ├── page.tsx
│   │   │   └── [slug]/page.tsx
│   │   ├── festival/                   # 카테고리 3 — 전국 축제·여행
│   │   │   ├── page.tsx
│   │   │   └── [slug]/page.tsx
│   │   └── blog/                       # AI 자동 생성 블로그
│   │       ├── page.tsx
│   │       └── [slug]/page.tsx
│   ├── components/
│   │   ├── layout/
│   │   │   ├── Header.tsx
│   │   │   └── Footer.tsx
│   │   ├── cards/
│   │   │   ├── EventCard.tsx
│   │   │   ├── SubsidyCard.tsx
│   │   │   └── FestivalCard.tsx
│   │   └── ads/
│   │       └── AdSenseBanner.tsx
│   └── lib/
│       ├── api/
│       │   ├── publicData.ts           # 공공데이터포털 API 클라이언트
│       │   ├── tourApi.ts              # 한국관광공사 TourAPI 클라이언트
│       │   └── kakaoLocal.ts           # 카카오 로컬 API 클라이언트 (추후)
│       ├── claude.ts                   # Anthropic Claude API 블로그 생성
│       └── utils/
│           └── expiry.ts               # 만료 처리 유틸리티
├── scripts/
│   ├── collect-incheon.ts              # 인천 지역 정보 수집
│   ├── collect-subsidy.ts              # 보조금·복지 정책 수집
│   ├── collect-festival.ts             # 축제·여행 정보 수집
│   └── generate-blog.ts               # AI 블로그 글 자동 생성
├── public/
│   └── data/
│       ├── incheon.json                # 수집된 인천 지역 정보
│       ├── subsidy.json                # 수집된 보조금 정보
│       ├── festival.json               # 수집된 축제 정보
│       └── blog.json                   # 생성된 블로그 글 목록
├── .github/
│   └── workflows/
│       └── daily.yml                   # 매일 07:00 KST 자동화 워크플로우
├── PROJECT_PLAN.md
├── PROJECT_MEMORY.md
├── next.config.ts
├── wrangler.toml                       # Cloudflare Pages 설정
└── package.json
```

## 환경변수

| 변수명 | 용도 |
|---|---|
| `ANTHROPIC_API_KEY` | Claude AI 블로그 자동 생성 |
| `PUBLIC_DATA_API_KEY` | 공공데이터포털 API 인증 |
| `TOUR_API_KEY` | 한국관광공사 TourAPI 인증 |
| `KAKAO_API_KEY` | 카카오 로컬 API 인증 (추후) |
| `NEXT_PUBLIC_ADSENSE_ID` | Google AdSense |
| `NEXT_PUBLIC_GA_ID` | Google Analytics |

## 자동화 흐름 (GitHub Actions — 매일 07:00 KST)

1. 공공데이터포털 API → 인천 지역 정보 수집 → `public/data/incheon.json` 갱신
2. 공공데이터포털 API → 보조금·복지 정책 수집 → `public/data/subsidy.json` 갱신
3. 한국관광공사 TourAPI → 축제·여행 정보 수집 → `public/data/festival.json` 갱신
4. 만료 항목 → `expired: true` 마킹 (페이지 유지, 목록에서 제외)
5. Claude AI → 오늘의 블로그 글 자동 생성 → `public/data/blog.json` 추가
6. Git 커밋 & 푸시 → Cloudflare Pages 자동 배포

## 수익화

- **Google AdSense**: 메인, 목록, 상세, 블로그 페이지에 광고 배치
- **쿠팡 파트너스**: 블로그 글 하단 배너

## 구현 단계

| 단계 | 내용 | 상태 |
|---|---|---|
| 1단계 | 프로젝트 세팅, Cloudflare 배포, GA/AdSense 연동 | 완료 |
| 2단계 | 카테고리 1~3 데이터 수집 스크립트 + 페이지 구현 | 진행 중 |
| 3단계 | Claude AI 블로그 자동 생성 | 예정 |
| 4단계 | 카테고리 4 (맛집) 카카오 API 연동 | 추후 |

---

## 향후 로드맵 (2026-08 갱신)

### 우선순위 0 — AdSense 재신청 (진행 중)
| 일정 | 내용 |
|---|---|
| 2026-08-15 | GSC 미색인 감소 확인 (빈 본문 페이지 채우기 효과) |
| 2026-08-21 | AdSense 재신청 (8/15 확인 결과 양호 시) |
| 2026-09 하순 | 빈 본문 backfill 소진 예상 → markdown 배치량 평시(2/2/5) 복귀 |

**이 기간에는 파이프라인을 크게 바꾸지 않는다.** 새 카테고리 추가나 프롬프트 대개편은
심사에 변수를 만들므로 재신청 이후로 미룬다.

### 우선순위 0.5 — 초이스 중복 글 정리 (2026-08-15 이후 착수)

**⚠️ 지금 하지 않는 이유**: GSC 미색인 감소를 확인 중이며(8/15 예정),
글을 삭제하면 색인된 URL이 404가 되어 오류가 늘고 **8/15 판단이 흐려진다.**
AdSense는 404 자체를 감점하지 않으므로 정리는 GSC 확인 이후로 미룬다.
대신 **신규 중복 차단은 2026-08-09에 선행 조치**했다.

**실태 (2026-08-09 조사)**
| 항목 | 수치 |
|---|---|
| `recommended-products.json` entries | 324건 |
| 고유 productId | 211종 |
| **중복 productId** | **58종** |
| 발행 초이스 글 | 130건 |

최다 반복:
- `7849328215` 라입 트리트먼트 — **9회** (04-18 ~ 08-08)
- `8631601073` 앤커 파워뱅크 — 8회 / `9523928845` 티르티르 쿠션 — 8회
- `9517050490` 드라이비아 마우스 — 6회 (06-18 ~ 08-06)

**원인**: `CHOICE_PRODUCT_HISTORY_DAYS` 가 7일인데 카테고리 로테이션도 7일 주기라,
조건 `경과일 < 7` 에서 **정확히 7일은 통과**했다. 반복 사례 대부분이 7일 간격이다.

**선행 조치 (2026-08-09 완료)**
- 제외 기간 7 → 30 (`58a59ff`) → 60 (`8cc36c4`)
- 후보 풀 상한 `CHOICE_TARGET_POOL_SIZE: '120'` 신설 (기본 50)
- 7/30 마우스 글 1건 삭제 (`072c4d7`)

**⛔ 영구 제외로 바꾸지 말 것**
카테고리별 고유 상품이 부족해 5~9개월 내 고갈되고
`"선정 가능한 상품이 3개 미만"` 에러로 매일 실패한다.

| 카테고리 | 고유 상품 |
|---|---|
| beauty-fashion | 22종 |
| digital | 23종 |
| health / pet-life | 27종 |
| kitchen / home-appliance-furniture | 32종 |
| living | 42종 |

풀을 늘려 고유 상품이 충분히 확보된 뒤에 재검토한다.

**착수 시 할 일**
1. 각 productId 그룹에서 **가장 오래된 1건만 남기고** 나머지 삭제
	 (최초 글이 색인됐을 가능성이 높고 후속은 재탕이다)
2. 삭제 후 sitemap 갱신 확인
3. 필요시 삭제 URL → 최초 글로 301 리다이렉트 검토 (Cloudflare Pages `_redirects`)
4. **`recommended-products.json` 이력은 삭제하지 않는다.**
	 이력이 남아야 해당 상품이 다시 선정되지 않는다 (§2-4 계열)
5. 삭제 대상 산출 방법은 `WORK_LOG.md` 2026-08-09 항목 참조

**⚠️ pre-push 훅 주의**: 초이스 글 삭제는 `check-worktree-safety.ps1` 이
삭제 파일 본문을 `git show` 로 읽으려다 차단하던 문제가 있었다.
`ab52932` 에서 `--diff-filter=d` 로 해결했으므로 현재는 정상 삭제 가능하다.

**함께 처리할 것 (미해결)**
- 초이스 요약문에 `"쿠팡 판매량 상위권 상품 중 중복 필터와 품질 필터를 통과한..."` 이
	그대로 노출된다. 내부 선정 로직이라 독자에게 의미가 없고 상업 신호만 강하며,
	목록 카드에도 표시되어 제목보다 문제가 크다.
- 초이스 글 생성 워크플로 점검. 2026-08-09에 VS Code가 자체 판단으로 본문을
	체험형·단정형("직접 경험해보면", "체감하는 분들이 많습니다")으로 무단 교체한 사례가 있다.

### 우선순위 1 — 콘텐츠 품질 개편 (AdSense 통과 후 착수)

**현재 진단**
- 모든 콘텐츠가 **API에서 긁어온 2차 정보**다. 보조금·축제·맛집 전부 남의 데이터이고
	사진도 남의 사진이라, "이 사이트만의 가치"를 설명하기 어렵다.
- 글이 천편일률적이고 AI로 쓴 흔적이 드러난다. 실제 방문자만 알 수 있는
	내부 묘사·음식 묘사가 없다.
- **이미지가 글 내용과 무관하다.** 보조금 글에 관광공사 랜드마크 사진이 붙는 구조라
	독자도 검색엔진도 연결성을 못 느낀다.

**⛔ 하지 말아야 할 방향**
- "실제 가본 것처럼" 쓰는 것. 안 가본 곳의 맛·인테리어를 지어내면 허위이며,
	구글이 요구하는 것은 "체험한 척"이 아니라 실제 유용성이다.

**✅ 갈 방향**
| 축 | 내용 |
|---|---|
| 정직한 포지션 | "에디터가 정리한 정보"로 문체 전환. 감성 문장 대신 신청 조건·서류·마감·유사 제도 비교 등 실용 정보 밀도를 높인다 |
| 발행량 조정 | 하루 5건 → 3건. 양보다 질 |
| 이미지 | 글 내용과 무관한 랜드마크 사용 재검토 |

### 우선순위 2 — 사찰기행 카테고리 신설 (AdSense 통과 후)

**근거**: 현재 사이트에 없는 **1차 콘텐츠(직접 촬영 + 실제 방문 경험)**를 확보한다.
E-E-A-T의 첫 E(Experience)에 해당하며, API 2차 정보 일변도에서 벗어나는 유일한 축이다.

| 항목 | 내용 |
|---|---|
| 소재 | 전국 유명 사찰의 약 2/3 방문분, 본인 촬영 사진 다수 보유 |
| 분량 | 주 1편 기준 **1년 이상** 지속 가능 |
| 저작권 | 본인 촬영이라 문제 없음 (현재 카테고리들의 최대 약점 해소) |
| 사진 연차 | 오래된 사진도 유효. 사찰은 변화가 적고, "계절별" 같은 앵글도 가능 |

**워크플로 (중요)**
1. Roy가 **메모 수준 초안**을 작성한다. 문장력은 필요 없다.
	 예: 방문 시기, 동선과 소요 시간, 인상 깊었던 지점, 사람 많고 적음, 사진 매수
2. AI는 **사실 정보(역사·건축·교통)를 보강하고 문장을 다듬는 역할만** 한다.
3. **감상과 경험은 반드시 Roy의 것을 사용한다.** AI가 지어내지 않는다.

이 구조가 기존 맛집 글과 결정적으로 다른 점: 맛집은 아무도 안 가봤는데 가본 척 써서
문제였고, 사찰은 실제로 다녀온 것을 표현만 도움받는다.

**착수 조건**: AdSense 통과 확인 후. 새 카테고리는 URL이 대량 추가되므로
GSC 색인 상황이 안정된 뒤에 시작한다. 급하게 3~4편만 올리면 미완성 카테고리로 보인다.

### 우선순위 3 — 비용 구조 대비

| 시점 | 내용 |
|---|---|
| 2026-08-31 | Sonnet 5 인트로 단가 종료. input $2→$3 / output $10→$15 (**1.5배**) |
| 2026-09-01~ | 현재 하루 약 970원 → **약 1,450원** 예상. 일일 예산 1,500원에 근접 |

**대응 선택지**: 예산 한도 1,500 → 2,000~2,200원 상향, 또는 발행량 축소(우선순위 1과 연계).
8월 말에 결정한다.
